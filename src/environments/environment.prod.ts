export const environment = {
  production: true,
  apiUrl: '/api',
  mongodb: {
    appId: 'your-app-id', // Replace with your MongoDB Realm App ID
    region: 'us-east-1'    // Replace with your app region
  }
};